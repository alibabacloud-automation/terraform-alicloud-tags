# Basic Tags Example

This example demonstrates the basic usage of the terraform-alicloud-tags module with minimal configuration.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

To clean up:

```bash
terraform destroy
```

## Cost

This example creates tag meta tags and associated rules, which are free services. 